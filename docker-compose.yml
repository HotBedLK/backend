
services:
  api:
    build: .
    ports:
      - "8000:8000"

  redis_database:
    image: "redis:alpine"
    command: redis-server --save 20 1 --loglevel warning --requirepass 1234
    ports:
      - "6379:6379"
    volumes:
      - cache:/data

volumes:
  cache:
    driver: local